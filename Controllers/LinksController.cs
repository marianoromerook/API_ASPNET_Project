using API_RESTful_Project.Models;
using API_RESTful_Project.Services;
using Microsoft.AspNetCore.Mvc;
/*
namespace API_RESTful_Project.Controllers
{
    [ApiController]
    [Route("api/admLinks")]
    public class LinksController : Controller
    {
       private readonly LinkService _linkService;

       public LinksController(ILinkService linkService) 
        {
            _linkService = (LinkService?)linkService;
        }
        /*
        [HttpPost]
       public IActionResult CreateLink([FromBody] LinkModel model)
        {
            if (ModelState.IsValid)
            {
                //  creación del enlace publicitario
                // ...
            }
            else
            {
                return BadRequest(ModelState);
            }
        }
    }
}*/
